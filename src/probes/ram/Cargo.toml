[package]
name = "ram"
version = "0.1.0"
edition = "2021"
description = "Program for RAM analysis"

[dependencies]
log = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sysinfo = "0.35"
chrono = "0.4"
