[package]
name = "board"
version = "0.1.0"
edition = "2021"
description = "Sub-module for main board / motherboard analysis"

[dependencies]
log = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"
