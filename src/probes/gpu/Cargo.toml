[package]
name = "gpu"
version = "0.1.0"
edition = "2021"
description = "Sub-module for GPU device analysis"

[dependencies]
log = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
nvml-wrapper = "0.11"
chrono = "0.4"
rusqlite = "0.36"

core = { path = "../../core" }